https://chatgpt.com/c/69a6f7bb-d998-83a7-80b4-1ef24e5756d1
现在其实我们已经差不多搞清楚了一帧要干什么了， 但是写起来很痛苦，很大原因是因为我们现在
的代码职责依然分工不明确， 比如Renderer这个东西， 又在当前端URP的 RendererAsset，又在
当后端的渲染线程， 这个是很有问题的。
比如RenderPath， 其实就是一个RenderPipeline，但是命名上很不直觉。
同时 管线中存在大量的 Update Tick等意义不明的操作， 无法让人一眼明白这个过程中干了什么。
整个流程其实还算比较清晰的， 每一帧固定的：

Game::TickFrame()
    RenderEngine::BeginFrame()  等待这一帧的framecontext可复用
    ResourceManager：：Update()  异步加载的资源
    SceneManager::GetInstance()->TickSimulation();
    SceneDelta delta = SceneManager::GetInstance()->FlushData();

    RenderEngine::BuildFrame(frameIndex, delta, view)
        CPUWorld.ApplyDelta();
        GPUWorld.ApplyDelta();
        这一步产生这一帧的BuildContext，和上文分离，产生只读数据

        RenderPipeline::Prepare(buildCtx)
        RenderPipeline::Record(cmdStream, buildCtx)
            PassA::Record(cmdStream, passCtx)
            PassB::Record(cmdStream, passCtx)
        这里不直接提交cmdStream， 而是录制， 用vector存储， 后续一股脑推进RenderBackEnd进行消化
    RenderEngine::SubmitFrame(frameIndex, cmdStream)
        RenderBackend::Enqueue(frameIndex, cmdStream)

这个地方， Resord阶段生成的cmdStream，其实就是我们之前想优化的renderop的集合， 因为一个个发会造成无锁队列的负担，减少发射粒度或许会减少等待时长？ 尝试一下。
然后在这个过程中， SUbmitFrame只是代表**这一帧上传完了** 这个非常重要， 就是不是在这个地方做提交， 而是这一帧的封口接口

    BuildFrame：可以录制、可以 flush chunk 到队列（只是搬运数据）
    SubmitFrame：只做一件事：发 “EndFrame” 标记（帧封口）

FrameSubmit（帧边界层）：只在 SubmitFrame() 发一个 EndFrameToken（或 EndFrameChunk）
只有它能触发 RT 的 Submit/Present/fence


//------------------------------------------------------------------------------------------
• 下一步我建议先把“一帧”彻底收口，再去跑 GPUScene。现在还没到直接冲 GPUScene 主链的时候，原因很具体：

  RenderEngine 这边虽然已经有 PrepareFrame() / BuildFrame() 了，但真正的“帧封口”还在 pipeline 里发。[Runtime/Renderer/
  RenderPath/LagacyRenderPipeline.cpp](/D:/GitHubST/TinyEngine/Runtime/Renderer/RenderPath/LagacyRenderPipeline.cpp#L19)
  到 #L23 里做了 SetFrame，#L53 直接 EndFrame()，这和你文档里“SubmitFrame 只发 EndFrameToken”的目标还是冲突的。对应地，
  [Runtime/Renderer/RenderEngine.cpp](/D:/GitHubST/TinyEngine/Runtime/Renderer/RenderEngine.cpp#L44) 现在还是
  PrepareFrame -> pipeline->Prepare -> UploadCopyOp，[Runtime/Renderer/RenderEngine.cpp](/D:/GitHubST/TinyEngine/
  Runtime/Renderer/RenderEngine.cpp#L99) 的 EndFrame() 也只清了 CPUScene，没有把 upload/fence 生命周期纳进去。

  更关键的是 upload 生命周期还没闭环。[Runtime/Renderer/UploadPagePool.cpp](/D:/GitHubST/TinyEngine/Runtime/Renderer/
  UploadPagePool.cpp#L47) 的 OnSubmitted() 和 #L61 的 Recycle() 目前没有被主链调用；这意味着你用 FrameTicket 做了页归
  属，但页并不会按 fence 真正回收。再加上 [Runtime/Renderer/FrameTicket.h](/D:/GitHubST/TinyEngine/Runtime/Renderer/
  FrameTicket.h#L31) 的 operator<= 实际写的是 <，等值 fence 不会回收，这里也会埋坑。

  所以顺序我建议这样排：

  1. 先整理“一帧”主链，不要先上完整 GPUScene。
     把 Game::TickFrame() 明确成 PrepareFrame -> RecordFrame -> SubmitFrame -> EndFrame。
     把 SetFrame/EndFrame 从 render pipeline 挪到 RenderEngine::SubmitFrame()，pipeline 只负责录制渲染命令。
     把 UploadCopyOp() 也视为录制阶段的一部分，不要让 pipeline 和帧边界互相穿透。
  2. 立刻补完 FrameTicket + UploadPagePool 闭环。
     复用 frame slot 前，先按旧 ticket 调 Recycle()。
     当前帧真正 submit 后，再用当前 FrameTicket 调 OnSubmitted()。
     顺手修掉 FrameTicket::operator<= 的比较错误。
  3. 然后跑一个“最小 GPUScene 路径”，不要一上来做完整 GPU culling。
     先验证 SceneDelta -> CPUScene shadow -> GPUScene copy op -> shader 读 object/material/proxy buffer 这条链是稳定的。
     第一阶段可以先不依赖 compute culling/indirect，只要能用 GPUScene 数据源正确画出来就够了。
     等这条链稳定，再恢复 [Runtime/Renderer/RenderPath/GPUSceneRenderPipeline.cpp](/D:/GitHubST/TinyEngine/Runtime/
     Renderer/RenderPath/GPUSceneRenderPipeline.cpp) 里的 compute/visibility/indirect 部分。

  如果按工程风险排序，我会把你后面的工作定成这三个里程碑：

  - M1：帧边界收口，SubmitFrame 成为唯一封口点。
  - M2：UploadPagePool 按 FrameTicket 正常提交和回收。
  - M3：跑通最小 GPUScene 渲染链，再接 GPU culling。

  结论就是：先继续整理“一帧”，但目标不是泛泛整理，而是把 submit/fence/upload recycle 这三个点做实；做完这个，再上
  GPUScene，成本会低很多。若你愿意，我下一步可以直接帮你把 RenderEngine 改成 Prepare/Record/Submit/EndFrame 这套骨架。

