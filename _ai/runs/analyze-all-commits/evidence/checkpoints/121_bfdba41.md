<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-121-bfdba41 -->
# Checkpoint 121: commit bfdba41

## 基本信息
- **Commit Hash**: bfdba41df9a79c26a2cc15e874d9aafe6fa1bfc6
- **Subject**: F: GPUScene中， dirty更新的逻辑修正， index问题
- **Author**: shentao
- **Date**: 2026-01-20 22:50:36 +0800

## 核心变更

### 1. 修复索引错误
在 `GPUSceneManager.cpp` 中修复 dirty 更新逻辑的数组索引问题：

#### 修复前（错误）
```cpp
perObjectDataBuffer[i] = {};  // 使用循环变量 i
perObjectTempData.push_back(perObjectDataBuffer[i]);  // 使用循环变量 i
```

#### 修复后（正确）
```cpp
perObjectDataBuffer[index] = {};  // 使用实际索引 index
perObjectTempData.push_back(perObjectDataBuffer[index]);  // 使用实际索引 index
```

### 2. 问题分析
在 dirty 更新逻辑中，循环使用 `i` 遍历所有对象，但实际写入位置应该使用 `index`（可能是可见对象的索引）。使用错误的索引会导致：
- 数据写入错误的位置
- 临时数据与实际数据不同步
- 渲染结果不正确

## 受影响模块
| 模块 | 文件 | 变更类型 |
|------|------|----------|
| Graphics | GPUSceneManager.cpp | Bug 修复 |

## 风险点
1. **无明显风险**：这是修复已确认的 Bug

## 可能引入的 Bug
1. **无**：这是修复而非引入新变更

## 建议调查范围
- 验证 dirty 更新逻辑在其他地方是否也有类似问题
- 测试对象增删场景下的索引正确性