<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-061-12ecade -->
# 检查点 061 - commit 12ecade
## 基本信息
- **Commit Hash**: 12ecade79a5aeeb59a64731f0d3d26142e37a63d
- **作者**: jerry sheen <jerryshin961115@gmail.com>
- **提交时间**: 2025-11-05 00:36:24 +0800
- **主题**: D： first version of MetaLoader & MetaSaver， 下一步需要适配这个过程，目前还是有点问题。

## 核心修改
### 1. 序列化基础设施
- 引入 **`nlohmann/json`**（`Runtime/External/nlohmann/json.hpp`，体积大）。
- 新增 **`Runtime/Serialization/`**：**`MetaData.h`**（`MetaData` / `TextureMetaData` / `MaterialMetaData` / `ModelMetaData`）、**`MetaFileSerialization.h`**、`BaseTypeAndEnumSerialization.h`（`to_json`/`from_json` 与部分 `NLOHMANN_JSON_SERIALIZE_ENUM`）。
- **`MetaLoader.h` / `MetaLoader.cpp`**（由原 **`MetaFile.cpp`** 迁出）：**`LoadMeta<T>`** 从磁盘读 JSON；**`MetaSaver.h`**：按相对路径写 **`.meta`**（去扩展名、`create_directories`、格式化 dump）。
- 示例资源 **`Assets/.../*.meta`**（材质/纹理等 JSON，路径仍为绝对盘符形式，与后续「相对路径」改造相关）。

### 2. 配套
- 多处以 **`MetaLoader`/`MetaData`** 替换原 `MetaFile` 包含关系；**`EditorMainBar`**、**`D3D12EditorGUIManager`**、**`EngineCore.vcxproj.filters`** 等同步调整。
- 文档：组件工厂说明、TODO 勾选；**`.vscode/settings.json`** 增加 C++ 标准库关联。

## 涉及模块
- Serialization、Resource、Graphics、Editor、Assets、Docs

## 风险点
1. **枚举映射笔误**：如 `TextureFormat` 与字符串对应在 `BaseTypeAndEnumSerialization.h` 中部分疑似复制错误，易导致反序列化错类型。
2. **meta 内绝对路径**：换机或 CI 会失效。
3. **仓库体积**：单次提交含完整 `json.hpp`。

## 可能的错误源
- `LoadMeta` 打开路径与 `PathSettings::ResolveAssetPath` 不一致；JSON 字段与 `from_json` 不匹配抛异常。
