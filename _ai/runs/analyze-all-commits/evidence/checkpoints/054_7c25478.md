<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-054-7c25478 -->
# 检查点 054 - commit 7c25478
## 基本信息
- **Commit Hash**: 7c2547857867c87e341ce82e8a6e247bdb98c941
- **作者**: jerry sheen <jerryshin961115@gmail.com>
- **提交时间**: 2025-11-01 20:34:00 +0800
- **主题**: D：第一版EditorGUI基础组件， 等待后续调整视口大小。

## 核心修改
### 1. 面板架构与目录调整
- **`EditorGUIManager`** 从头文件 `Editor/Windows/` 迁至 **`Editor/EditorGUIManager.h`**，实现合并到 **`Editor/EditorGUIManager.cpp`**（删除 `Windows/EditorGUIManager.cpp`）。
- 引入 **`EditorPanel`** 基类及 **`EditorGameViewPanel`、`EditorHierarchyPanel`、`EditorMainBar`**；`InitPanel()` 中创建并加入 `mPanelList`。
- **`EditorGUIManager::Render()`**（非虚）：遍历 `mPanelList` 调 `DrawGUI()`，最后 **`ImGui::Render()`**；D3D12 子类不再实现独立 `Render()` 演示窗口逻辑。

### 2. EditorSettings
- 新增 **`EditorSettings.{h,cpp}`**，存放各面板宽高等静态配置（初版占位为主）。

### 3. 其它
- `Game.cpp` / `Renderer.h` 中 include 改为 **`"EditorGUIManager.h"`**。
- **`Projects/imgui.ini`** 新增，记录窗口布局；`EngineCore.vcxproj.filters` 增加 Editor\Panel 等条目。

## 涉及模块
- Editor 层文件重组、Panel 初版、ImGui 集成、工程过滤器

## 风险点
1. **Hierarchy 中硬编码 `io.DisplaySize = 1920x1080`**，与真实窗口/ DPI 不一致时布局与点击区域易错位（提交说明也提到待调视口）。
2. **EditorMainBar** 等源文件注释存在乱码（编码问题），不影响逻辑但影响维护。

## 可能的错误源
- 面板析构顺序与 `mPanelList` 释放；`EditorGUIManager` 析构中已 `delete` 各 panel。
