<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-157-cbc20e9 -->
# Checkpoint 22: cbc20e9

## Commit 标题
D： 解决GPUScene+ bindless下shader对应问题， 先不走这条路径

## 核心改动
1. **新增配置合法性检查**：
   - 在Game.cpp的初始化逻辑中新增断言：当`RenderSettings::s_RenderPath`为`GPUScene`且`RenderSettings::s_EnableVertexPulling`为`false`时，触发断言失败，阻止程序启动
   - 注释说明问题原因：当前GPUScene渲染路径只适配了Bindless Vertex Pulling模式，非Vertex Pulling模式下的shader选择逻辑存在问题，暂时禁用该配置组合
2. **保留默认配置**：
   - ProjectSettings中`RenderSettings::s_EnableVertexPulling`默认值保持为true，正常使用GPUScene路径不受影响
   - 仅当用户手动关闭Vertex Pulling并使用GPUScene路径时，才会触发断言，提示配置不合法

## 涉及模块
- 游戏核心初始化 (`Runtime/Core/Game.cpp`)
- 渲染设置 (`Runtime/Settings/ProjectSettings.cpp`)

## 风险点
无，仅为配置检查，不影响正常的合法配置运行

## 推测错误原因
当前GPUScene渲染路径的shader生成和选择逻辑是基于Vertex Pulling（Bindless顶点拉取）模式开发的，还没有适配传统的顶点缓冲区绑定模式；如果用户关闭Vertex Pulling并使用GPUScene路径，会导致shader选择错误，出现渲染异常甚至崩溃；通过新增断言，提前阻止这种非法配置，避免用户踩坑，后续修复了shader选择逻辑后可以移除该断言
