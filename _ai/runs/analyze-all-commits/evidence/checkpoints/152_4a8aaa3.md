<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-152-4a8aaa3 -->
# Checkpoint 27: 4a8aaa3

## Commit 标题
D: 去掉不需要的FrameContext

## 核心改动
1. **移除FrameContext冗余类**：
   - 完全删除`FrameContext.cpp`文件，移除约300行冗余代码
   - FrameContext类原本用于管理帧上下文数据，经过架构重构后，其职责已经被其他模块（如RenderBackend、RenderEngine）拆分接管，不再需要单独的FrameContext类

2. **相关接口适配**：
   - RenderBackend.cpp/h中移除对FrameContext的引用和依赖
   - RenderCommand.h中移除与FrameContext相关的定义
   - 所有使用FrameContext的地方都已经替换为新的实现方式，确保功能不受影响

## 涉及模块
- 渲染引擎核心 (`Runtime/Renderer/FrameContext.cpp`、`Runtime/Renderer/RenderBackend.cpp/h`、`Runtime/Renderer/RenderCommand.h`)

## 风险点
无，仅为冗余代码清理，不影响现有功能

## 推测错误原因
随着引擎架构的不断迭代和重构，FrameContext类的职责已经被拆分到其他模块中，成为冗余代码；本次提交将其移除，简化代码结构，减少不必要的复杂度，使渲染引擎的架构更加清晰。
