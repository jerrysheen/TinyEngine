# Checkpoint 086: 31b1b4c

## Commit 信息
- **Hash**: `31b1b4c48359b2ab55dc0235fdc18c43ec87dddc`
- **Subject**: D: 修改渲染管线，继续梳理逻辑同步 -> 全场景脏更新 -> 脏数据同步逻辑
- **Date**: 2025-12-22 09:59:30 +0800
- **Author**: shentao

## 核心变更
1. **渲染管线重构**: 梳理逻辑同步 -> 全场景脏更新 -> 脏数据同步逻辑
2. **LagacyRenderPath**: 新增 `LagacyRenderPath.h` (41行)，传统渲染路径
3. **GPUSceneRenderPath**: 新增 `GPUSceneRenderPath.h` (23行)，GPU Scene 渲染路径
4. **IRenderPath 抽象**: 新增 `IRenderPath.h` (12行)，渲染路径接口抽象
5. **GPUCulling Shader**: 新增 `GPUCulling.hlsl` (94行)，GPU 剔除计算 shader
6. **ComputeShader**: 新增 `ComputeShader.h/cpp` (23行)，计算 shader 支持
7. **LinearAllocator**: 新增 `LinearAllocator.h` (48行)，线性分配器
8. **每帧脏数据拷贝**: Lagacy RenderPath 每帧将脏数据拷贝到全局 Buffer

## 涉及的模块
- **Runtime/Renderer/RenderPath**: `IRenderPath.h` (12行), `LagacyRenderPath.h` (41行), `GPUSceneRenderPath.h` (93行)
- **Runtime/Renderer**: `RenderEngine.h/cpp` (25行), `Renderer.h/cpp` (18行), `RenderContext.cpp`, `RenderCommand.h` (28行), `RenderAPI.h` (7行), `RenderStruct.h` (16行), `RenderUniforms.h`
- **Runtime/Graphics**: `ComputeShader.h/cpp` (23行), `GPUSceneManager.h/cpp` (115行), `LinearAllocateBuffer.h/cpp` (19行)
- **Runtime/Platforms/D3D12**: `D3D12RenderAPI.cpp` (160行)
- **Runtime/GameObject**: `MeshRenderer.h/cpp` (12行)
- **Runtime/Core**: `LinearAllocator.h` (48行)
- **Assets/Shader**: `GPUCulling.hlsl` (94行)
- **Docs**: GPUScene实现方案.canvas, 渲染融入梳理.canvas

## 风险点
1. **双渲染路径复杂性**: Lagacy 和 GPU Scene 两条路径需要维护两套逻辑
2. **GPUCulling Shader 新实现**: 94 行新 shader 需要充分测试
3. **脏数据同步逻辑**: 全场景脏更新的触发和同步时机
4. **大规模架构变更**: 32 文件变更，642 新增/105 删除，净增 537 行

## 可能的错误原因
1. **脏标记遗漏**: 某些数据修改未正确标记为 dirty
2. **双路径切换问题**: Lagacy 和 GPU Scene 路径切换时的状态混乱
3. **GPUCulling 结果错误**: GPU 剔除计算结果与 CPU 不一致
4. **ComputeShader 调度错误**: 计算 shader 的线程组配置或 barrier 问题
5. **数据同步时序**: 脏数据拷贝到全局 buffer 的时机与 GPU 使用冲突
