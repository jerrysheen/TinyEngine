# Checkpoint 003: 2b614ed

## Commit 标题
D：默认build脚本设置， 跑通基本结构。

## 核心改动
1. **构建与运行脚本优化**：
   - 新增 `WinBuildAndRun.bat` 批处理脚本，60行代码，实现一键编译并运行引擎，简化开发流程
   - 新增 `.vscode/settings.json` 配置文件，52行VSCode相关配置，统一开发环境设置
   - 更新 `EngineCore.vcxproj.filters` 项目过滤器文件，15行配置，调整项目文件结构展示

2. **游戏核心类框架搭建**：
   - 新增 `Runtime/Core/Game.cpp` 和 `Runtime/Core/Game.h` 核心类，共56行代码，实现游戏引擎的核心控制类框架，包含初始化、主循环、销毁等基础接口
   - 更新 `Runtime/Entry.cpp` 入口文件，新增4行代码，集成Game类，完成程序启动到游戏主循环的流程打通
   - 更新 `Runtime/PreCompiledHeader.h` 预编译头文件，新增1行基础头文件引用

## 涉及模块
- 构建系统
- 开发环境配置
- 引擎核心框架

## 风险点
- WinBuildAndRun.bat脚本可能存在路径问题，在不同环境下可能无法正常执行
- Game类框架只是基础结构，尚未实现具体功能，后续扩展需要保持接口兼容性

## 推测错误原因
完成了引擎最基础的运行结构搭建，实现了从启动到主循环的完整流程，并且提供了一键构建运行脚本，方便后续开发调试。这是引擎核心功能开发的起点，建立了最基础的运行框架。
