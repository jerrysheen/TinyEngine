<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-055-464dd30 -->
# 检查点 055 - commit 464dd30
## 基本信息
- **Commit Hash**: 464dd304db95061f00a87ff190898f303409f49a
- **作者**: jerry sheen <jerryshin961115@gmail.com>
- **提交时间**: 2025-11-01 23:49:10 +0800
- **主题**: D: BaseGUI上传， 等待制作GUI逻辑，最主要是Hirarchy，以及Inspector里面修改的部分。

## 核心修改
### 1. 扩展面板与布局数据
- 新增 **Console / Inspector / Project** 面板类，并在 **`InitPanel`** 中注册；主菜单窗口标题 **ZXEngine → TinyEngine**（与窗口标题一致）。
- **`EditorSettings`** 大幅扩展：用 **`Vector2`** 存各面板 **归一化** 起止位置与尺寸，附长注释描述 1920×1080 布局；提供 **`GetMainBarPanelStartPos/Size`、`GetGameViewPanelStartPos/EndPos`** 等访问器（与 FinalBlit 等对接）。

### 2. 视觉与运行时
- **D3D12EditorGUIManager** 初始化里用浅灰/白底 ImGui 配色替代纯 `StyleColorsDark`。
- **`FinalBlitPass`**：在 **EDITOR** 下视口改为 **GameView 区域**（`EditorSettings::GetGameViewPanelStartPos/EndPos`），非 Editor 仍全屏窗口。
- **`WindowSettings`** 默认分辨率改为 **1920×1080**；**`WindowManagerWindows`** 标题改为 **TinyEngine**。
- **`Vector2`** 增加 **`operator+`、 `operator*`**（逐分量），供布局计算。

### 3. 工程与配置
- `imgui.ini` 更新多窗口位置；`EngineCore.vcxproj.filters` 纳入新 panel 源文件。

## 涉及模块
- Editor Panel、EditorSettings、FinalBlitPass、ProjectSettings/窗口、Vector2、imgui.ini

## 风险点
1. **GameView 视口与 ImGui 窗口** 需严格对齐，否则最终画面与 UI 框分离。
2. **布局写死为 1920×1080 比例**，窗口缩放后需随 `EditorSettings` 重算（注释已预留比例思路）。

## 可能的错误源
- `GetGameViewPanelEndPos` 与像素对齐、DPI 缩放未统一时裁切或黑边。
