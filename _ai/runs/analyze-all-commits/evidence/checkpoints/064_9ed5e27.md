<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-064-9ed5e27 -->
# 检查点 064 - commit 9ed5e27
## 基本信息
- **Commit Hash**: 9ed5e276d91aca64ad8c988d0fde5f59622b71d3
- **作者**: jerry sheen <jerryshin961115@gmail.com>
- **提交时间**: 2025-11-08 19:54:17 +0800
- **主题**: D: 优化序列化和反序列化代码，Component类全走虚函数实现序列化反序列化

## 核心修改
### 1. 序列化框架重命名与收敛
- `MetaSaver` 演进为 `JsonSerializer`（提供统一 JSON 读写接口），`EditorMainBar` 调用全面替换。
- `MetaFileSerialization.h` 更名为 `AssetSerialization.h`，`BaseTypeAndEnumSerialization.h` 更名为 `BaseTypeSerialization.h`。
- `ScriptFactory.h` 重构并更名为 `ComponentFactory.h`，统一脚本/组件工厂语义。

### 2. 组件改为虚函数序列化
- `Component` 及具体组件（`Camera`、`Transform`、`MeshFilter`、`MeshRenderer`、`MonoBehaviour` 等）调整接口：通过组件对象自身虚函数完成序列化/反序列化。
- 样例 `house.meta` 中字段结构同步变化（如 `AssetHandle` 拆分为 `MeshHandle` / `MatHandle`，脚本类型由 `Script` + `ScriptName` 转为具体类型名）。

### 3. 清理与配套
- 删除/替换旧的 `GameObjectAndComponentSerialization.h` 大块静态模板逻辑，向组件自描述迁移。
- 工程过滤器与 `MetaFactory` / `MetaLoader` 适配新头文件命名和接口。
- `bin-int` 下生成物在此提交被清零（`*.obj.enc` 从有内容到 0），属于构建产物噪声。

## 涉及模块
- `Runtime/Serialization/*`
- `Runtime/GameObject/*`、`Runtime/Scripts/CameraController.*`
- `Editor/Panel/EditorMainBar.*`
- `Assets/Temp/house.meta`

## 风险点
1. 大规模重命名+接口迁移，旧调用点极易遗漏（编译通过后仍可能运行时行为不一致）。
2. 旧 meta 文件与新 schema 兼容性不足，需要迁移脚本或版本标识。
3. `JsonSerializer::SvaeAsJson` 拼写沿用旧 typo，后续可读性和 API 稳定性较差。

## 可能的错误源
- 工厂注册键与 JSON `Type` 字段不一致，导致 `MetaFactory` 创建失败。
- 组件虚函数未覆盖完整字段，出现“能加载但状态不完整”的隐性问题。
