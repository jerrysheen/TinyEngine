<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-114-55f612d -->
# Checkpoint 114: commit 55f612d

## 基本信息
- **Commit Hash**: 55f612d7a70e0bda7b72b02cd42adfba82d836b9
- **Subject**: D： 上传配置， 通过ProjectSettings设置当前走什么渲染路径， 注意，当前不支持 VertexPulling + LagacyRenderPath
- **Author**: shentao
- **Date**: 2026-01-13 17:45:18 +0800

## 核心变更

### 1. 新增渲染路径配置系统
在 `ProjectSettings.h/cpp` 中新增 `RenderSettings` 类：
```cpp
class RenderSettings
{
public:
    enum class RenderPathType { Legacy, GPUScene };
    static RenderPathType s_RenderPath;
    static bool s_EnableVertexPulling;
};
```

### 2. 新增 VertexPulling 专用着色器
- 新增 `StandardPBR_VertexPulling.hlsl`：使用 `SV_VertexID` 从 GPU 缓冲区读取顶点数据
- 修改 `StandardPBR.hlsl`：移除 VertexInput 参数，改为从 Vertex Buffer 读取

### 3. 统一配置管理
将硬编码的 `enableVertexPulling` 变量替换为 `RenderSettings::s_EnableVertexPulling`：
- `BatchManager.cpp/h`：移除成员变量 `enableVertexPulling`
- `GPUSceneRenderPass.cpp/h`：移除成员变量 `enableVertexPulling`
- `D3D12RenderAPI.cpp`：根据配置条件注册 LargeVertexBuffer
- `BistroSceneLoader.cpp`：根据配置选择加载哪个着色器

### 4. 渲染路径选择逻辑
- `RenderEngine.cpp`：根据 `s_RenderPath` 选择 Legacy 或 GPUScene 渲染路径
- `Camera.cpp`：根据 `s_RenderPath` 选择 OpaqueRenderPass 或 GPUSceneRenderPass

### 5. 安全断言
在 `Game.cpp` 中添加启动断言：
```cpp
ASSERT(!(RenderSettings::s_EnableVertexPulling == true && 
         RenderSettings::s_RenderPath == RenderSettings::RenderPathType::Legacy));
```

## 受影响模块
| 模块 | 文件 | 变更类型 |
|------|------|----------|
| Settings | ProjectSettings.h/cpp | 新增配置类 |
| Shader | StandardPBR.hlsl | 修改 |
| Shader | StandardPBR_VertexPulling.hlsl | 新增 |
| Renderer | BatchManager.cpp/h | 修改 |
| Renderer | GPUSceneRenderPass.cpp/h | 修改 |
| Renderer | RenderEngine.cpp | 修改 |
| Platform | D3D12RenderAPI.cpp | 修改 |
| Scene | BistroSceneLoader.cpp | 修改 |
| Core | Game.cpp | 新增断言 |
| Graphics | GPUTexture.h | 新增占位 |

## 风险点
1. **配置组合限制**：VertexPulling + LegacyRenderPath 不兼容，需要运行时断言保护
2. **着色器变体切换**：场景加载时根据配置选择不同着色器，需确保资源正确加载
3. **RootSignature 动态配置**：VertexPulling 模式下需要额外绑定 LargeVertexBuffer

## 可能引入的 Bug
1. 如果 `s_EnableVertexPulling` 和 `s_RenderPath` 配置不一致，可能导致渲染错误
2. 着色器切换时可能存在缓存或编译问题
3. 断言失败会直接终止程序，影响用户体验

## 建议调查范围
- 渲染路径切换相关的资源生命周期管理
- VertexPulling 模式下的性能表现
- 配置系统的持久化和编辑器支持