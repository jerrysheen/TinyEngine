<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-058-10f1477 -->
# 检查点 058 - commit 10f1477
## 基本信息
- **Commit Hash**: 10f1477373c27bedba5d87eff878542a68855000
- **作者**: jerry sheen <jerryshin961115@gmail.com>
- **提交时间**: 2025-11-02 21:19:01 +0800
- **主题**: F： 删除Scene的时候， 应该在Scemanager做nullptr，而不是Scene中反调用

## 核心修改
- **`Scene::~Scene`**：移除析构函数内对 **`SceneManager::SetCurrentScene(nullptr)`** 的反调用（避免 Scene 析构阶段再碰 Manager，顺序/重入风险）。
- **`SceneManager`**：在 **`DeleteScene`（或等价删除路径）** 中，若被删的 **`scene` 正是 `mCurrentScene`**，则 **先将 `mCurrentScene = nullptr`**，再 **`delete scene`** 与从 map 移除。

## 涉及模块
- `Runtime/Scene/Scene.cpp`、`SceneManager.cpp`

## 风险点
- 其它路径若仍假设「删场景时 Scene 自己会清空当前指针」，需同步改为只在 Manager 侧维护。

## 可能的错误源
- 析构顺序：Scene 先析构时调用已部分销毁的 `SceneManager`；或 **UAF**：当前场景指针仍指向已释放对象。
