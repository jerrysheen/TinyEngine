# Checkpoint 081: 32aac28

## Commit 信息
- **Hash**: `32aac2825172b2131bba14f345ff50fd2ebaf72b`
- **Subject**: D: 第一版PSO和RootSig优化，减少RootSignature的绑定次数
- **Date**: 2025-12-11 22:35:50 +0800
- **Author**: shentao

## 核心变更
1. **RootSignature Cache**: 新增 `D3D12RootSignature.h/cpp` (135行)，实现 RootSignature 缓存复用
2. **PSO 优化**: 优化 PSO 创建和绑定逻辑
3. **RenderStruct 扩展**: 新增 `RenderStruct.h` (58行)，定义渲染数据结构
4. **绑定插槽明确化**: 用更明确的绑定插槽替代动态配置
5. **Shader 简化**: `SimpleTestShader.hlsl` 删除 32 行，简化测试 shader

## 涉及的模块
- **Runtime/Platforms/D3D12**: `D3D12RootSignature.cpp` (7行), `D3D12RootSignature.h` (128行，核心), `D3D12RenderAPI.cpp` (208行重构)
- **Runtime/Renderer**: `RenderStruct.h` (58行，新增), `RenderCommand.h`, `RenderAPI.h/cpp`
- **Runtime/Graphics**: `Material.cpp` (4行), `Material.h`, `IGPUBufferAllocator.h`
- **Runtime/Core**: `PublicEnum.h` (10行删除), `PublicStruct.h` (15行)
- **Assets/Shader**: `SimpleTestShader.hlsl` (32行删除), `Core.hlsl` (44行)

## 风险点
1. **RootSignature Cache 正确性**: 缓存键生成是否能正确区分不同 RootSignature
2. **绑定插槽冲突**: 明确化后的插槽分配可能出现冲突
3. **删除 PublicEnum 10 行**: 可能影响其他依赖该枚举的代码
4. **Cache 生命周期**: RootSignature 缓存的释放时机管理

## 可能的错误原因
1. **Cache Key 冲突**: 不同 RootSignature 被错误地认为是相同的
2. **Binding Slot 越界**: D3D12 的绑定插槽数量有限制 (128 root parameters)
3. **Shader 与 RootSignature 不匹配**: Core.hlsl 变更后需要对应更新 RootSignature
4. **Cache 内存泄漏**: RootSignature 对象未正确释放
