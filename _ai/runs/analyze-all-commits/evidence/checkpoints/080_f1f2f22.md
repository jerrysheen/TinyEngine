# Checkpoint 080: f1f2f22

## Commit 信息
- **Hash**: `f1f2f22103034984a652f38bfcad7f62b3aac983`
- **Subject**: D: 第一版，All Object Data + Raw Material Buffer Data，调整Material部分，重新整理RootSignature参数
- **Date**: 2025-12-10 23:16:40 +0800
- **Author**: shentao

## 核心变更
1. **数据扁平化**: 去掉 MaterialPropertyBlock 设计，所有数据扁平化存储
2. **Material 重构**: `Material.cpp` 127行重构，`Material.h` 20行更新
3. **MaterialLayout**: 新增 `MaterialLayout.h` (81行)，定义材质数据布局
4. **MaterialInstance**: 新增 `MaterialInstance.h` (31行)，材质实例化支持
5. **删除 MaterialData**: 删除 `MaterialData.h/cpp` (36行)，数据合并到 Material
6. **CoreAssert**: 新增 `CoreAssert.h` (18行)，核心断言系统
7. **RootSignature 重整理**: 重新规划 RootSignature 参数布局

## 涉及的模块
- **Runtime/Graphics**: `Material.cpp` (127行重构), `Material.h` (20行), `MaterialLayout.h` (81行，新增), `MaterialInstance.h` (31行，新增), `GPUSceneManager.cpp` (29行), `PersistantBuffer.cpp`
- **Runtime/Core**: `CoreAssert.h` (18行，新增), `PublicEnum.h`, `PublicStruct.h`, `PreCompiledHeader.h` (18行)
- **Runtime/Platforms/D3D12**: `D3D12RenderAPI.cpp` (205行重构), `D3D12Struct.h`
- **Runtime/Renderer**: `RenderAPI.h`, `RenderEngine.cpp`, `RenderUniforms.h`, `Renderer.cpp`, `FinalBlitPass.cpp`, `RenderPass.h`
- **Runtime/GameObject**: `MeshRenderer.h/cpp` (11行)
- **Runtime/Scene**: `Scene.cpp`, `SceneManager.cpp`
- **Docs**: 12月_Material重构.md (298行文档)
- **删除**: `MaterialData.h`, `MaterialPropertyBlock.h/cpp`, `MaterialData.cpp`

## 风险点
1. **Material 数据兼容性**: 扁平化后的数据格式与旧资源不兼容
2. **MaterialPropertyBlock 删除**: 69行删除可能影响依赖该功能的代码
3. **RootSignature 变更**: 重新整理可能导致 shader 与 runtime 不匹配
4. **大规模重构**: 34 文件变更，613 新增/470 删除，净增 143 行

## 可能的错误原因
1. **材质数据布局不匹配**: MaterialLayout.h 与 shader 中的常量缓冲区布局不一致
2. **MaterialInstance 引用问题**: 新材质实例系统的生命周期管理
3. **删除代码残留引用**: MaterialData 和 MaterialPropertyBlock 的残留引用
4. **RootSignature 缓存失效**: 变更后旧的 PSO/RootSignature 缓存需要刷新
