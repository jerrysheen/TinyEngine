<!-- Created by TinyEngine harness, task analyze-all-commits, step checkpoint-150-a784a00 -->
# Checkpoint 29: a784a00

## Commit 标题
D: 开始修改Tick一帧的代码流程，降低耦合，明确命名
1. Renderer改为RenderBackend，明确为后端调用。
2. RenderPath改为RenderPipeline，明确未来持有Pass

## 核心改动
1. **命名规范统一与架构清晰化**：
   - 将`Renderer`类重命名为`RenderBackend`，明确其作为渲染后端调用入口的职责，避免与渲染器概念混淆
   - 将`RenderPath`系列类重命名为`RenderPipeline`，明确其作为渲染管线的职责，未来将持有多个RenderPass
   - 对应的文件也同步重命名：`Renderer.cpp/h` → `RenderBackend.cpp/h`，`*RenderPath.cpp/h` → `*RenderPipeline.cpp/h`
   - 移除旧的`IRenderPath.h`接口，新增`IRenderPipeline.h`接口作为渲染管线的统一抽象

2. **帧流程重构准备**：
   - 开始修改一帧的Tick代码流程，降低各个模块之间的耦合度
   - 明确各个模块的职责边界，为后续的帧流程优化打下基础
   - 更新架构图文档，反映最新的命名和架构调整

3. **相关模块适配**：
   - RenderEngine、RenderCommand、FrameContext等模块都适配新的命名和接口
   - GPUScene模块适配新的RenderBackend接口
   - 所有RenderPass类（FinalBlitPass、GPUSceneRenderPass、OpaqueRenderPass等）适配新的RenderPipeline架构
   - 项目工程文件同步更新，适配文件重命名

## 涉及模块
- 渲染引擎核心 (`Runtime/Renderer/{Renderer.cpp => RenderBackend.cpp}`、`Runtime/Renderer/{Renderer.h => RenderBackend.h}`、`Runtime/Renderer/RenderEngine.cpp/h`、`Runtime/Renderer/RenderCommand.h`、`Runtime/Renderer/FrameContext.cpp`)
- 渲染管线 (`Runtime/Renderer/RenderPath/{IRenderPath.h => IRenderPipeline.h}`、`Runtime/Renderer/RenderPath/{GPUSceneRenderPath.cpp => GPUSceneRenderPipeline.cpp}`、`Runtime/Renderer/RenderPath/{GPUSceneRenderPath.h => GPUSceneRenderPipeline.h}`、`Runtime/Renderer/RenderPath/{LagacyRenderPath.cpp => LagacyRenderPipeline.cpp}`、`Runtime/Renderer/RenderPath/LagacyRenderPipeline.h`)
- 渲染通道 (`Runtime/Renderer/RenderPipeLine/FinalBlitPass.cpp`、`Runtime/Renderer/RenderPipeLine/GPUSceneRenderPass.cpp`、`Runtime/Renderer/RenderPipeLine/OpaqueRenderPass.cpp`、`Runtime/Renderer/RenderPipeLine/RenderPass.cpp`)
- GPU场景管理 (`Runtime/Scene/GPUScene.cpp`)
- 架构文档 (`Docs/TODO/Tick流程重构26_3_8.md`、`Docs/设计/新的Tick流程.canvas`)

## 风险点
1. 大量文件和类重命名，可能存在引用遗漏的情况，导致编译错误
2. 接口调整可能影响依赖这些接口的模块，需要确保所有适配都正确完成
3. 这是帧流程重构的开始，后续还会有更多的改动，需要确保当前改动的稳定性

## 推测错误原因
原来的命名存在概念模糊、职责不清晰的问题，Renderer和RenderPath的命名不能准确反映其职责，导致架构理解成本高，模块耦合度高；本次重构通过统一命名规范，明确各个模块的职责边界，降低耦合度，为后续的帧流程优化和功能扩展打下良好的基础。
